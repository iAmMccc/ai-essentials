---
name: ui-diff
description: |
  对比 UI 设计稿和 App 截图，检查元素间距、位置、对齐、颜色、字号等差异。
  当用户提供设计稿和截图要求对比时使用。
---

# UI 对比 Skill

对比 UI 设计稿与 App 实际截图，找出视觉差异。

## 使用方式

```
/ui-diff <设计稿路径> <截图路径>
```

两个参数都是图片文件路径（支持 PNG、JPG 等格式）。

如果用户只提供了一个路径，询问另一个。如果用户提供的是目录，读取目录下的图片文件并让用户选择。

## 对比维度

按以下维度逐项检查，**只报告有差异的项**，没问题的不要列出来：

### 1. 布局与间距
- 元素之间的间距是否一致（上下左右）
- 内边距（padding）和外边距（margin）
- 整体布局结构是否匹配

### 2. 对齐
- 水平对齐：左对齐、居中、右对齐
- 垂直对齐：顶部、居中、底部
- 多个元素之间的对齐关系

### 3. 尺寸
- 元素宽高是否匹配
- 图片/图标尺寸
- 按钮、输入框等控件尺寸

### 4. 文字
- 字号大小
- 字重（粗细）
- 行高 / 行间距
- 文字颜色
- 文字内容是否一致

### 5. 颜色与样式
- 背景色
- 边框颜色和粗细
- 圆角大小
- 阴影效果
- 透明度

### 6. 其他
- 缺失的元素（设计稿有但截图没有）
- 多余的元素（截图有但设计稿没有）
- 状态栏、安全区域适配

## 输出格式

```markdown
## UI 对比结果

### 概览
- 设计稿：{文件名}
- 截图：{文件名}
- 差异数量：{N} 处

### 差异详情

#### 1. {问题简述}
- **位置：**{元素在页面中的位置描述}
- **设计稿：**{设计稿中的表现}
- **实际截图：**{截图中的表现}
- **建议修改：**{具体的修复方向}

#### 2. {问题简述}
...

### 总评
{一句话评价整体还原度，如「整体还原度较高，主要问题集中在间距细节」}
```

## 注意事项

- 基于视觉判断，间距/尺寸的数值是估算，标注为「约 Xpx」
- 如果设计稿和截图的分辨率/比例不同，先说明这一点
- 优先报告影响视觉效果明显的差异，细微的放后面
- 如果截图包含状态栏等系统 UI，对比时忽略系统 UI 部分
- 不要编造差异，看不清的标注「无法确认」
